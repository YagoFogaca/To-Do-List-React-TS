import styled from 'styled-components';

export const InputClass = styled.input`
  font-size: 1.6rem;
  padding: 0.9rem 0.8rem;
  cursor: text;
  border: 1px solid #000;
  &:focus-visible {
    outline: 0.5px auto #f66b0e;
  }
`;
